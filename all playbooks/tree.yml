https://docs.ansible.com/projects/ansible-core/2.16/collections/ansible/builtin/yum_module.html
---
- name: Install tree package on all nodes
  hosts: all
  become: true
  tasks:
    - name: install tree package
      yum:
        name: tree
        state: removed
