---
- name: Install httpd package on all hosts 
  hosts: all
  become: true #become: true activates privilege escalation, allowing Ansible to execute tasks with administrative or root privileges on the target system.
  tasks:
  - name: Install httpd package
    yum:
      name: httpd
      state: installed
    notify: start apache #tells a task to trigger a specific background operations task (called a "handler") named start apache

  handlers:
  - name: start apache
    service:
      name: httpd
      state: started
